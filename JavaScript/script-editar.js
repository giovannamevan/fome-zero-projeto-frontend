// Selecione o botão pelo ID
var editarDoadorButton = document.getElementById("editar_doador");

// Adicione um ouvinte de eventos para o clique no botão
editarDoadorButton.addEventListener("click", function () {
    // Redireciona o navegador para a página desejada
    window.location.href = "../Usuário/Doador/editar_produtos";
});

